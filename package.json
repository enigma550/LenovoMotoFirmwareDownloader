{
  "name": "lenovo_moto_firmware_downloader",
  "type": "module",
  "private": true,
  "scripts": {
    "// --- Daily Development ---": "",
    "start": "bun run scripts/launch-dev.ts",
    "start:native": "LE_MOTO_RENDERER_LINUX=native bun run start",
    "start:cef": "LE_MOTO_RENDERER_LINUX=cef bun run start",
    "start:clean": "bun run dev:data:reset && bun run start",
    "start:instance": "bunx electrobun dev",
    "// --- Build & Distribution ---": "",
    "build:dev": "bun run qdl:prepare && bun run wdi:prepare && bun run prepare:all && bunx electrobun build --env=dev",
    "build:canary": "bun run qdl:prepare && bun run wdi:prepare && bun run prepare:all && bunx electrobun build --env=canary",
    "build:stable": "bun run qdl:prepare && bun run wdi:prepare && bun run prepare:all && bunx electrobun build --env=stable",
    "// --- Infrastructure & Utils ---": "",
    "prepare:all": "bun run web:build && bun run scripts/sync-frontend.ts",
    "qdl:prepare": "bun run scripts/prepare-qdl.ts",
    "wdi:prepare": "bun run scripts/prepare-wdi-simple.ts",
    "web:build": "cd web && bun run build:desktop",
    "dev:data:reset": "bun run scripts/reset-dev-data.ts",
    "arch:layers": "bun run scripts/check-layer-boundaries.ts",
    "arch:cycles": "bun run scripts/check-dependency-cycles.ts",
    "arch:check": "bun run arch:layers && bun run arch:cycles",
    "check": "bun run arch:check && bun run tsc --noEmit && bun run biome:check",
    "biome:format": "bunx --bun @biomejs/biome format --write",
    "biome:lint": "bunx --bun @biomejs/biome lint --write",
    "biome:check": "bunx --bun @biomejs/biome check",
    "biome:fix": "bunx --bun @biomejs/biome check --write",
    "biome:ci": "bunx --bun @biomejs/biome ci"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.4",
    "@types/bun": "latest",
    "rcedit": "^5.0.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/archiver": "^7.0.0",
    "archiver": "^7.0.1",
    "electrobun": "^1.14.4"
  }
}
